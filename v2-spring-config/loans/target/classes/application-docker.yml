server:
  port: 8090
spring:
  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/loans
    driverClassName: org.postgresql.Driver
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
